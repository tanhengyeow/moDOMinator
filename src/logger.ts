export function logError(message: any) {
  // eslint-disable-next-line no-console
  console.error('[moDOMinator] ' + message);
}

export function logInfo(message: any) {
  // eslint-disable-next-line no-console
  console.log('[moDOMinator] ' + message);
}
